import React from "react";
import ReactDOM from "react-dom/client";
import CommunityBoard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CommunityBoard />
  </React.StrictMode>
);
